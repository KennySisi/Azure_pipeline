from setuptools import setup, find_packages

setup(
    name='azure_pipeline',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your project dependencies here
    ],
)
